name: Main Final Test
#TESTTTT 

on: 
  push:
    branches: ['main']

jobs:
  stage-sync:
    uses: ./.github/workflows/Final-Test.yml
    with:
      BRANCH: 'main'
      ENVIRONMENT: stage
      VERIFICATION: www.google.com
    secrets: inherit
